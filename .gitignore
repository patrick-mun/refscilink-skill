# System
.DS_Store
Thumbs.db

# Dependencies
node_modules/

# Environment
.env
*.log

# Claude Code
.claude/

# RefSciLink — backups created by build_references.mjs and the skill
backup/

# RefSciLink — references.json is regenerated by build_references.mjs and tests.
# The canonical copy lives in data/reference_bibliographique/json/references.json
# and is committed intentionally when it represents a stable fixture state.
# Uncomment the line below only if you want to exclude it entirely:
data/reference_bibliographique/json/references.json
